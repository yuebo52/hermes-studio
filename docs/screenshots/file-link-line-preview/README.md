# File-link line preview visual evidence

1. `01-inline-file-link.png` — Full chat context before opening the file. The previewable document is a lightweight inline link with no download-card affordance.
2. `01-inline-file-link-detail.png` — Actual-size detail of the dotted-underlined file reference.
3. `02-line-range-preview.png` — The same link after activation. The right side panel is open, `L550–552` is in view, and the requested range is highlighted.

The screenshots are generated by the mocked Playwright flow in `tests/e2e/generated-file-preview.spec.ts`; they contain no live account data or credentials.
